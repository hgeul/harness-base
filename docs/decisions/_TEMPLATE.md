# ADR: <결정 제목>

> branch: `<브랜치>` | date: `<YYYY-MM-DD>` | status: 제안 | 채택 | 폐기 | 대체됨(→ 어떤 ADR)
> related progress: `docs/progress/<YYYY-MM-DD_작업명>.md`
>
> 이 파일은 `/grill` 산출물이다 (grill-with-docs). "왜 이렇게 만들었나"(의도부채)를 박제한다.
> 단순 실행 결과는 progress에 남긴다. 이 템플릿은 대안·가정·위험·재검토 조건이 필요한 결정에만 사용한다.

## 1. 맥락 (왜 지금 이 결정을 하나)

- 트리거: <PR 코멘트 / 운영 이슈 / 외부 스펙 변경 등>
- 관련 코드: `path/to/File.ext:line`
- 관련 룰·정책: `.claude/rules/<...>.md`, `docs/policy/<...>.md`

## 2. 결정과 근거

| 결정 항목 | 결정안 | 근거 (왜) | 버린 대안 |
|---|---|---|---|
|  |  |  |  |

## 3. 기대는 가정 / 받아들인 위험

전제가 틀리면 결정이 깨진다.

- 가정 (틀리면 깨지는 것): <...>
- 받아들인 위험: <...>

## 4. 검증 (이 결정을 무엇이 담보하나)

- build-time: <테스트 / dod 항목>
- run-time: <헬스 / 스모크 / 재현 시나리오>

## 5. 만료 조건 (언제 재검토하나)

코드 주석 "잠정"이 영원히 남지 않도록 재검토 트리거를 박는다.

- <예: 외부 코드 카탈로그 확정 시 재검토>

## 6. /grill 문답 로그

> 질문(AI) / 답(사람) 원문. 다음 세션이 Read 로 재활용.

- Q1:
  - A1:
